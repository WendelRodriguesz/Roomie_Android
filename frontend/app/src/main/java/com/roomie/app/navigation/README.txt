Navigation (Compose)
- AppNavHost: registra rotas das features.
- Rotas em um sealed class/obj (Routes).
- Cada feature expõe uma função <Feature>NavGraph(builder).
Boa prática: UDF (estado desce, eventos sobem).
